export interface Producto {
    id: number;
    nombre: string;
    talla: string; // Reemplazado de 'peso' por 'talla'
    imagen: string;
    categoria: string; // Reemplazado de 'tipo' por 'categoria'
}
